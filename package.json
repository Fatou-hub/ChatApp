{
  "name": "chatapp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm run dev-client\" \"npm run dev-server\"",
    "dev-client": "npm run dev --prefix client",
    "dev-server": "npm run dev --prefix server",
    "install": "npm install --omit=dev && npm install --prefix client --omit=dev && npm install --prefix server --omit=dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "nodemon": "^3.1.9",
    "vite": "^6.0.7"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "express": "^4.21.2"
  }
}
